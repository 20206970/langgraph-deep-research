# P2.7 验收记录

## 执行信息

- 日期：2026-08-26
- 验收脚本：`scripts/p2_7_acceptance.py`
- 论文样例：`2023iscience.pdf`（本地文件，不进入仓库）
- 执行命令：

```text
python scripts/p2_7_acceptance.py <path-to-2023iscience.pdf>
```

该脚本使用真实论文 PDF 和真实的 SQLite/FTS5、Chroma、MarkItDown PDF 解析与父子分块；embedding、reranker 使用确定性本地 fake，VLM 不调用外部服务。脚本只输出计数、状态和布尔结果，不输出论文正文、图片内容、私有存储路径或认证信息。

## 受控验收结果

| 项目 | 结果 |
| --- | --- |
| PDF 转 Markdown | 通过，MarkItDown PDF fallback |
| Markdown 字符数 | 53,743 |
| 物理父块 | 29 |
| 子块 | 95 |
| 真实论文检索命中 | 8 个父块 |
| 正常 reranker | `applied` |
| reranker 故障降级 | `degraded` |
| 删除后检索 | 0 个父块 |
| 恢复后检索 | 8 个父块 |
| 30 天清理 | 通过 |
| 跨用户检索 | 0 个父块 |
| 私有内容日志脱敏 | 通过 |
| Chroma 文件句柄释放 | 通过 |

## 环境限制

该论文预检为 17 页、约 10 张图片。当前机器没有 Docling 的 Hugging Face 布局模型缓存，首次加载 Docling 时下载超时，因此本次选择了真实 MarkItDown PDF 文本降级路径。该路径按设计不提取图片，结果中的 `image_count=0`、`vision_status=not_configured` 是明确的降级状态，不代表图片链路已经完成验收。

VLM 和 BGE reranker 没有配置真实模型服务或权重；VLM 图片描述和真实 reranker 推理仍需在部署服务器上配置后复验。已有单元测试覆盖 VLM schema、单图失败、真实 provider 构造、reranker 正常和降级分支。

## 隐私审查

- 研究事件中的 `evidence_excerpt`、正文、Markdown、图片和路径字段统一脱敏。
- LangSmith 默认关闭内容采集；包含私有文档范围的运行强制隐藏输入和输出。
- API 不返回 `source_path`，引用只保留文档标题、页码/章节和稳定来源标识。
- 删除后 SQLite/FTS5 和 Chroma 均不再返回文档；恢复后重新可检索；过期清理删除向量和私有目录。

## 结论

P2.7 的自动化回归和文本优先的真实论文受控验收通过。Docling 图片抽取、VLM 视觉增强和真实开源 reranker 的最终验收依赖部署环境中的模型缓存/配置，当前不能将这三项描述为已完成的真实模型验收结果。
